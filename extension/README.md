# QINGYUAN Selection Guard

A user-triggered Manifest V3 browser extension. It analyses only selected text in the active tab, pauses media, dims recommendation sections and inserts a voluntary attention break. It has no host permissions, no remote endpoint and no token collection.

Load the `extension` directory as an unpacked extension in a Chromium-compatible browser. Review the source before use.
